---
description: Do NOT use the browser subagent for any reason
---

# No Browser Access

**NEVER use the `browser_subagent` tool in this workspace.** The user does not allow browser access.

For verification of web UI changes:
- Ask the user to check visually and report back
- Or rely on build/compile checks via terminal commands
